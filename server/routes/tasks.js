import express from "express";

const router = express.Router();

// Placeholder - Vee will implement
router.get("/", (req, res) => {
  res.json({ message: "Tasks route - Vee will implement" });
});

export default router;
